# Deprecated database-catalog recommendation UI

This directory preserves the frontend that displayed products from the retired
database catalogue. It is not imported by the active application.

The active UI presents live OpenAI web-search results, source links, loading and
failure states, and allergen evidence.
